name: Waka Readme

on:
  schedule:
    # Runs at 12am IST
    - cron: '30 18 * * *'
  workflow_dispatch:
jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          ### content
          SHOW_LINES_OF_CODE: "True"
          SHOW_PROFILE_VIEWS: "False"
          SHOW_SHORT_INFO: "False"
          SYMBOL_VERSION: "1"
          SHOW_LOC_CHART: "False"
          SHOW_TOTAL_CODE_TIME: "False"
          SHOW_UPDATED_DATE: "True"
          PDATED_DATE_FORMAT: "%Y.%m.%d %H:%M:%S"
          SHOW_TIMEZONE: "False" ### cannot collect data (why?)
          SHOW_EDITORS: "False" ### cannot collect data (why?)
          SHOW_PROJECTS: "False" ### cannot collect data (why?)
          SHOW_LANGUAGE: "False" ### cannot collect data (why?)
          SHOW_OS: "False" ### cannot collect data (why?)
          ### commit
          COMMIT_BY_ME: "True"
         
          
          
